name: Input workflow



on:
  push:
  workflow_dispatch:
    inputs:
      debug:
        type: boolean
        default: false

jobs:
  Inputs:
    runs-on: self-hosted
    steps: 
      - name: Inputs
        run: echo "inputs"
    
